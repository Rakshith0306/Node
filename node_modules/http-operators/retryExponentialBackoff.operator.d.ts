import { Observable, SchedulerLike } from 'rxjs';
/**
 * Repeats underlying observable on a timer
 *
 * @param maxTries The maximum number of attempts to make, or -1 for unlimited
 * @param initialWait Number of seconds to wait for refresh
 */
export declare const retryExponentialBackoff: (maxTries?: number, initialWait?: number, scheduler?: SchedulerLike) => <T>(source: Observable<T>) => Observable<T>;
export declare const retryWhenExponentialbackoff: (maxTries?: number, initialWait?: number) => <T>(source: Observable<T>) => Observable<T>;
